# Common-base practices audit

- **Repo:** `model-quality-gate`
- **Catalog id:** Hrz4 (package `model_quality_gate`, env prefix `AI_QUALITY`, profile var `AI_QUALITY_PROFILE`)
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)
- **Authoritative source:** reconciled to the maintainer's cross-repository audit matrix,
  authoritative on verdicts.
- **Note:** Hrz4 is the catalog's **eval / red-team promotion gate** (rule R5): it is the
  promotion AUTHORITY the verticals call, not a consumer of a gate. So E1/E2 are judged as
  "correct gate of record" (per-vertical metric bundles, fail-closed unknown-metric
  rejection, dataset ingest, the `/v1/gate` endpoint), not as a smoke split. Several
  `[agentic]` checks (B2, C3, C4) are N-A because Hrz4 runs no grounded product LLM of its
  own and processes no customer data. Each check was re-run against the current tree with
  the `model_quality_gate` package / `AI_QUALITY` env prefix substituted.

Applicability: Hrz4 ships a UI (`ui/`) and Terraform (`infra/terraform/`), so `[ui]` and
`[infra]` checks apply. **Load-bearing** checks (a FAIL breaks a shared catalog guarantee)
are A1-A6, C1-C5, D1-D3 and E1.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `grep -rE "google\|fastapi\|httpx\|pydantic\|boto3\|azure" src/model_quality_gate/domain/` returns nothing. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | 13 Protocol classes across `ports/*.py`, each with `@runtime_checkable` (13 decorators), all re-exported from `ports/__init__.py`; asserted by `test_port_parity.py::test_all_protocols_are_runtime_checkable`. |
| **A3** Swappable profiles by one config value `[all]` **(load-bearing)** | PASS | `AI_QUALITY_PROFILE = gcp\|local\|platform\|onprem`; per-port `adapters:` map in `config/settings.yaml`; `platform` is a tested hybrid (four explicit bindings, GCP elsewhere) and unknown profiles fail closed; offline suite runs with no google-cloud SDK (252 passed, 19 skipped, 4 integration deselected). |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `test_port_parity.py::test_adapter_constructs_with_single_settings_arg` parametrises `SDK_FREE_PROFILES` (`onprem`, `local`) x all 13 ports. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | `grep -n "^from google\|^import google" src/model_quality_gate/adapters/gcp/*.py` returns nothing; the offline leg imports every module with no SDK installed. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | Structural parity remains green, and `test_port_protocols_matches_settings_adapters` adds the set-equality drift guard (an unregistered `settings.adapters` key, or a `PORT_PROTOCOLS` entry with no binding, fails loudly, failing on BOTH directions). `tests/contract/test_behavioral_parity.py` proves behavioral parity across the knowledge-base and audit ports (identical domain objects / byte-identical records), the eval scorer is deterministic across reruns and fails fast on onprem, and the full eval-gate pipeline (golden dataset -> EvalReport) runs deterministically under `local` and fails fast under `onprem` with only a profile change. |
| **A7** Kernel vs vertical split in the domain `[all]` | N-A | Hrz4 is a horizontal platform, not a per-vertical fork target, so the kernel-vs-vertical fork boundary does not apply; all domain types live in one `domain/models.py`. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PASS | `adapters/platform/remote_audit.py` (50), `remote_knowledge_base.py` (76), `remote_registry.py` (103): marshalling only. `COMPLIANCE.md` R2/R4/R6 map each horizontal (Hrz5 audit/trace, Hrz3 registry, Hrz2 KB) to a `platform` binding; no second in-repo implementation of a horizontal. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | The promotion verdict is a pure stdlib function: `EvalReport.passed = all(r.passed)`, `EvalMetricResult.passed = score >= threshold` (`evaluation_service.py:175`), gate `passed = eval.passed and redteam.passed` (`gate_service.py:106`); bars in `domain/thresholds.py`. The LLM judge only scores metrics; the domain decides. Replayed offline by `eval/run_eval.py` driving the real `PromotionGateService`. |
| **B2** Every claim carries a citation; empty retrieval is a hard error `[agentic]` | N-A | Hrz4 emits eval / gate / model-card artifacts, not a grounded product narrative of cited claims; the KB pull is best-effort reference warm-up never returned to a caller. Empty golden dataset is a hard error (`EmptyDatasetError`, `evaluation_service.py:96`). |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `GateReviewPolicy` (`domain/hitl.py`): a borderline PASS (any metric within 0.02 of its bar) or a marginal red-team block forces `requires_human_review`, auditing as `ESCALATED`; escalation only raises the bar; the gate emits a verdict and executes no promotion itself. Covered by `test_policies.py`. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | PASS | `config/settings.yaml` has a `policy:` section carrying `borderline_margin` and the per-bundle bars; `domain/policy.py` `PromotionPolicy.from_policy` layers it over the reference constants in `domain/thresholds.py`, and the API (`get_promotion_policy` -> `thresholds_for`) and the gate's maker-checker band (`build_gate_service(review_policy=...)`) both read it. Defaults reproduce reference behaviour and an override changes the verdict, both asserted in `tests/unit/test_promotion_policy.py`. Overrides are fail-closed (unknown bundle / metric / key, non-numeric or out-of-range bar all raise at load). Residual: adding a NEW vertical bundle is still a code change in `thresholds.py` (it also needs metric scorers). |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | Engines are typed on plain `str` metric names with the metric vocabulary open via bundle dicts (`test_metric_bundles.py`); `ThinkingLevel` / `RedTeamCategory` / `Severity` / `Decision` are `StrEnum` (via the shared `hex-service-kit` commons), members ARE their wire values. Residual (tracked under B4-style config policy, not B5): extending bundles edits `thresholds.py` rather than config. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | Request schemas carry no `actor` field (`api/schemas.py:66-79`, documented "No actor field"); `get_principal` resolves a server-verified `Principal` via the IdentityPort on every route (`api/security.py:51`); the audit actor is `principal.actor`. The IAP refusal is now split by whose failure it is, and the split is read at the API. In `adapters/gcp/iap_identity.py` the `AI_QUALITY_IAP_AUDIENCE` check already ran before the assertion header was read, so what changed here is the type it raises and the answer that follows: `IapAudienceUnconfiguredError`, not a plain `IdentityError`; the lazy google-auth import moved inside a `try` and raises `IapVerifierUnavailableError`. Both subclass `EndUserAuthUnavailableError` (declared in `ports/identity.py`, carrying `http_status`) and both answer 503, while an absent or blank assertion header stays a plain `IdentityError` and a bare 401 "authentication required" that tells a caller nothing they could forge with. The audience is read in three states, so `AI_QUALITY_IAP_AUDIENCE` set and emptied is reported as emptied rather than as absent. `ctx.header(...)` is now `.strip()`ped, so a header a proxy or a deployment template rendered blank is refused as MISSING instead of reaching the algorithm pin. `api/security.py::get_principal` orders the `EndUserAuthUnavailableError` branch BEFORE the `IdentityError` branch, and the order is load-bearing: it is a subclass, so the reverse silently swallows it and answers the 401 the whole split exists to avoid. `tests/unit/test_iap_refusal_split.py` guards all of it. RED first, offline against the adapter exactly as it shipped: an unset audience gave `IdentityError: AI_QUALITY_IAP_AUDIENCE is not configured; cannot verify IAP assertion` whether or not the caller presented an assertion, and `api/security.py` answered 401 "authentication required" to it, sending an operator after a credential no credential would have supplied; a `"   "` header gave `IdentityError: assertion is not a compact JWS or JWE, so it carries no readable algorithm header`, a malformed token reported for an absent one; and with the `[gcp]` extra blocked, `ModuleNotFoundError: No module named 'google.auth.transport'` escaped `resolve` and `get_principal` for a bare 500 per request. The new module does not import against that tree at all; with only the two `Iap*Error` types injected and the old behaviour left standing it scored 8 failed / 3 passed, and reordering the two `except` branches on the fixed tree leaves 10 passed and fails `test_the_api_answers_a_deployment_failure_with_its_own_status_and_message` alone, 401 where 503 is owed. The fourth divergence from the `adapters/gcp/identity.py` family, the `principals` tuple, was investigated and deliberately NOT changed: this adapter grants `user:<subject>` and that family grants nothing, and it is this one that agrees with `hex_service_kit.federation.principal_from_iap_claims`, so aligning it would strip a verified identity of its own principal and widen nothing. The divergence is recorded in the test module rather than closed in the wrong direction. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | N-A | Single-tenant S2S control plane: its resources (targets, golden datasets, model cards, audit) are the operator's own MRM evidence, not per-customer tenant data, so there is no object-level / cross-tenant isolation surface; access is gated at the service tier (S2S allowlist) plus end-user identity for audit. NOTE: the seeded cross-tenant persona and KB `acl_tags` are an identity / demo layer only, the local KB `retrieve(query, top_k)` performs no ACL filtering and no route does per-resource entitlement; if cross-vertical evidence isolation is later required, data-tag ACLs must be added. |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | N-A | Hrz4 processes no customer PII (`AuditEvent` docstring `models.py:317-324`, `COMPLIANCE.md` P-04): only synthetic probe / golden text reaches the judge, so there is no boundary-redaction step. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | N-A | No runtime redactor and no home-jurisdiction PII pack in-repo; `pii_safety` is enforced as a promotion threshold each vertical's own eval must clear, not by an Hrz4-owned PII detector. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | CORS is `hex_service_kit.cors_allowlist` (explicit `AI_QUALITY_CORS_ORIGINS`, never `*`; dev-origin fallback ONLY under the local profile); `main()` binds via `resolve_bind_host` (loopback under the no-auth local profile unless `AI_QUALITY_ALLOW_INSECURE_DEMO=1`); Makefile `API_HOST ?= 127.0.0.1`. Existing config fail-closed stands (unknown metric/bundle 422, unknown adapter profile rejected, empty dataset 422, dataset_id mismatch 422, unknown persona 401, S2S secure OIDC + allowlist). Dockerfile CMD binds 0.0.0.0 by design (container runs profile=gcp behind platform ingress). |
| **C6** Security-header baseline on every surface `[ui]` | PASS | API: the middleware is the shared `hex_service_kit.web.add_security_headers` (adopted, not copied), emitting CSP `frame-ancestors` + conditional `X-Frame-Options`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, and HSTS on every non-local profile. UI: `ui/next.config.mjs` emits a full default-deny CSP (`default-src 'self'`, `base-uri`, `form-action`, `object-src 'none'`, `connect-src` scoped to self + the `NEXT_PUBLIC_API_BASE` origin only, no external host anywhere) plus nosniff, Referrer-Policy and HSTS. Asserted by `tests/unit/test_security_headers.py`. Console CSP is nonce-based: `script-src 'self' 'nonce-<n>' 'strict-dynamic'` from the policy module, the nonce set on the REQUEST `Content-Security-Policy` header in `ui/proxy.ts`, and `app/layout.tsx` forced dynamic so the route can carry it. The gate is `ui/scripts/assert-hydratable.mjs`, which executes the built page rather than merely reading the policy, so a CSP that blocks hydration (a header-only assertion would not catch this) fails the build. `assertHydratableCsp` refuses the half-configured build. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | INBOUND: `require_service_caller` is the shared `hex_service_kit.web.make_require_service_caller`, same env names + profile rule, on EVERY non-health route including all three `/v1/datasets` routes. OUTBOUND: the three platform delegates validate their base URL at construction (`_s2s.validate_base_url`, https-only outside loopback) and attach the S2S bearer + optional signed actor via `_s2s.headers()` (`HRZ_S2S_TOKEN` / `HRZ_S2S_SIGNING_KEY`, headers `X-Aiq-Actor`/`-Sig`), sourced from the commons. |
| **C8** Web login flow hardening `[ui]` | N-A | Hrz4 owns no web login flow; secure identity is the GCP IAP-injected assertion verified server-side (`adapters/gcp/iap_identity.py`), not an in-repo OIDC Authorization-Code login. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | `LocalAppendOnlyAuditAdapter` wraps the shared `hex_service_kit.audit.HashChainedAuditLog`: SHA-256 chain over canonical JSON, UPDATE/DELETE rejected by triggers, JSONL export/restore with per-line verification, `verify_chain()` exposed, honest-limits docstring. Proven by `tests/unit/test_audit_chain.py` (round-trip + in-place tamper detected). Managed profile keeps the locked WORM bucket (`logging_worm.tf`). |
| **C10** No secret values in the repo `[all]` | PASS | `config/settings.yaml` stores only `*_env` names + `${ENV:-default}` interpolation and the `kms_key` env pointer; the literal-secret grep is clean. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | Committed `requirements-dev.lock` + `requirements-gcp.lock` (uv pip compile, py3.12), ruff pinned exactly, Dockerfile installs from the lock; the commons package is pinned by tag in `pyproject.toml`, with an exact SHA in both locks. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | Base image digest-pinned, Actions SHA-pinned, `.github/dependabot.yml` present, `pip-audit` a hard CI gate. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | `ci.yaml` sets `AI_QUALITY_PROFILE: local`, `eval-gate.yaml` sets `onprem`; locked dev dependencies install with no google-cloud SDK and CI runs ruff + mypy + 252 tests + the self-eval, live presenter, portability, and clean-copy rename gates; no `secrets.` references. **Gate scope:** CI lints and format-checks the whole tree (`ruff check .`, matching the local gate), and the only ruff exclusion is `.agents/skills/`, the verbatim resync of the maintainer's shared agent skills, which this repo does not own. `tests/unit/test_gate_scope.py` enforces that: it rejects any exclusion broader than that path, asserts the set of tracked `*.py` files hidden from `ruff check .` is exactly the vendored ones, and fails if a vendored file starts referencing the `model_quality_gate` package (which would mean it is no longer an upstream copy). |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | Multi-stage `Dockerfile`: `build-essential` only in the builder, slim runtime; `USER appuser` (uid 10001); `HEALTHCHECK` on `/healthz`; `EXPOSE 8084`; `AI_QUALITY_PROFILE=gcp` set in the image. |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PARTIAL (posture-as-code complete; live enforcement proof outstanding) | Complete offline: ONE residency allowlist enforced at three points, all from `var.allowed_regions` / `allowed_regions` in `config/settings.yaml` (a) `terraform plan` (`variables.tf` `contains(var.allowed_regions, var.region)`), (b) Org Policy `gcp.resourceLocations` plus `iam.disableServiceAccountKeyCreation` (`org_policy.tf`), (c) application load (`config.validate_residency` / `ResidencyError`, so a service repointed by an env var refuses to start). VPC-SC is DRY-RUN FIRST (`use_explicit_dry_run_spec = !var.vpc_sc_enforce`, `vpc_sc_enforce` defaults false, enforced `status` block created only on opt-in). Posture alerts on VPC-SC violations and resourceLocations denials (`posture_alerts.tf`). Per-service CMEK (`kms.tf`) and >= 2557-day WORM retention. `terraform fmt -check` + `init -backend=false` + `validate` run as a credential-free CI job and as `make tf-check`. All of it asserted by `tests/unit/test_residency_posture.py`. **Still open (BLOCKED offline):** live enforcement evidence, i.e. the Org Policy applied to a named project, the perimeter promoted out of dry run with clean violation logs, and an actual CMEK/retention lock. That needs a real GCP project and cannot be produced here. The hosted CI job is also unproven because the GitHub account is billing-blocked; it was verified locally via `make tf-check`. |
| **E1** Offline eval gate of record; per-vertical bundles + ingest `[agentic]` **(load-bearing)** | PASS | Gate of record: `eval/run_eval.py` drives the REAL `PromotionGateService` offline (fakes, no GCP creds) on every PR via `ci.yaml` + `eval-gate.yaml`; 12 named per-vertical `METRIC_BUNDLES`; fail-closed unknown metric/bundle -> `UnknownMetricError` -> HTTP 422 (`thresholds.py:139`, `api/app.py:132`); `DatasetStorePort` + `POST /v1/datasets` ingest; `POST`/`GET /v1/gate`. Hrz4 IS the authority the verticals call. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | Every bundle carries a safety-family metric at the strictest 0.99 bar (`thresholds.py`); the offline gate has a dedicated `score_safety` proving a safety-below-threshold target can NEVER be a PASS (`run_eval.py:276`); the fail-closed `UnknownMetricError` means an unknown metric raises rather than silently scoring 0.0 as a false green; the gate combines eval AND red-team. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | Golden datasets are synthetic scenarios; `DEMO.md:14` warns "The golden datasets and red-team probes are fictional / synthetic. Do not gate [a real model] without ... sign-off". |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo` -> `scripts/model_quality_gate_demo.py` + `scripts/render_model_quality_gate_ui.py` under `AI_QUALITY_PROFILE=local` (real gate, no cloud / API key); `make demo-server` runs the live presenter-controlled server offline. |
| **F2** Demo cannot rot silently `[all]` | PASS | `make demo-selftest` starts the real presenter server on ephemeral loopback and asserts exact `data-demo-step`/verdict markers plus `/state` 0 -> 1 -> 2 -> 0 while driving candidate -> real gate -> related references -> restart. It validates metric/probe invariants and prevents aggregate PASS from rendering subreport FAIL; CI runs it offline. Retrieved context is display-only, never claimed as causal score provenance. |
| **F3** Portability claim is executable `[all]` | PASS | `make portability-demo` and CI gate the 13-port matrix and full GCP/platform-hybrid lazy construction; two identical local gates; dataset/card reopen; verified analyst/approver actors reaching audit; audit JSONL export/import equality, chain validity and interior-tamper rejection; on-prem fail-fast; and unknown-profile rejection. Its JSON explicitly excludes live cloud/sibling calls and complete on-prem/data/identity/infra/UI/policy/regulator migration. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | `SPEC.md` declares itself the authoritative build contract ("This file is the contract; the other docs describe how the pieces fit together"); no shipped feature is described as unbuilt (no stale "forthcoming / not built" claims found). Lighter than a full ordered AGENTS.md chain, but the "kept true" risk is clean. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | `COMPLIANCE.md` maps every principle P-01..P-12 and rule R1..R6 to a concrete control with file pointers, plus an honest gaps section, and now ends with a **Regulator crosswalk (ADOPTER-OWNED)** appendix: nine MAS (Singapore) obligations mapped to the internal principle, the control here, and an openable artefact, with an explicit ownership statement (a fork replaces the rows and upstream merges never overwrite it) and a 'what this does not claim' note separating posture-as-code from live enforcement evidence. |
| **G3** Documented, mechanised fork path `[all]` | PASS | `docs/ADOPTING.md` compares consume/adapter/fork modes and states institution-owned controls. `scripts/rename_fork.py` previews by default, preflights Ruff and validates names/collisions before writes, preserves canonical `.agents/skills`/upstream URLs, and renames package/CLI/service/distribution plus `.env.example` variables. A no-tool test proves zero mutation; `make rename-selftest` applies a clean copy, performs a fresh locked install, and runs its full gate. |
| **G4** Retired `[all]` | N-A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | PASS | `docs/faq/` covers features, security, portability, adoption and compliance, with the responsibility split across Hrz1/Hrz2/Hrz3/Hrz5/Hrz7 and institution-owned approvals; README links the index. |
| **G6** Contribution docs cover full extension touch list `[all]` | PASS | `CONTRIBUTING.md` documents "Add a port the right way" (Protocol, re-export from `ports/__init__.py`, gcp + onprem + platform bindings, single-settings constructor) and the adapter conventions; enforced by `tests/contract/test_port_parity.py`. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS | Every Markdown file the repo owns is at 0 em-dashes. `tests/unit/test_markdown_discipline.py` makes it a regression guard rather than a habit: it parametrises over every non-vendored `*.md`, asserts zero em-dashes, and asserts every ```mermaid fence is closed and non-empty. |

**Verdict counts:** 33 PASS, 1 PARTIAL, 0 FAIL, 7 N-A (41 checks). Load-bearing (A1-A6, C1-C5, D1-D3,
E1): 12 PASS, 0 PARTIAL, 0 FAIL, 3 N-A (C2, C3, C4). **No load-bearing FAIL or PARTIAL
remains**; the E1/E2 gate-of-record checks and both C7 directions are PASS. The single
remaining PARTIAL (D5) is code-and-config complete and blocked only on a named production
deployment.

## Gaps carried to systems/

Load-bearing: none open.

Quality-of-adoption (open):

- **D5 (partial, and it can go no further offline): live enforcement proof.** The
  posture-as-code half is complete (see the D5 row). What remains needs a named production
  deployment and cannot be produced from this repo: the `gcp.resourceLocations` Org Policy
  actually applied to a real project, the VPC-SC perimeter promoted out of dry run after a
  clean violation-log period, a real CMEK binding and an irreversibly locked retention
  policy. The credential-free `terraform fmt`/`validate` CI job is written and passes
  locally (`make tf-check`); it has not been observed on a hosted runner because the
  GitHub account is billing-blocked, so no job can start.

- **Gate scope (open, and it belongs upstream).**
  `.agents/skills/find-next-priorities/scripts/priority_inputs.py` is byte-identical to the
  maintainer's upstream copy of the same script and carries a ruff `I001` plus a format
  difference. It is excluded from this repo's gate because it is a vendored upstream copy, not
  because it is red: fixing it here would fork it from upstream and from every other repo that
  vendors the same file, so the fix belongs upstream followed by a resync. Nothing this repo owns is excluded, and `tests/unit/test_gate_scope.py` fails if
  that changes.

By design (N-A): A7 (horizontal platform, no per-vertical fork boundary), B2 / C3 / C4
(eval platform, no grounded product LLM, no customer PII), C2 (single-tenant control plane,
no per-customer tenant data), C8 (no in-repo web login; identity is the verified IAP
assertion).
